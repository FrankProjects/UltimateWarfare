{% extends 'game.html.twig' %}

{% block title %}Ultimate Warfare - Market{% endblock title %}

{% block content %}
    <p class="text-center">
        <a class="B" href="{{ path('Game/Market') }}">Buy</a> -
        <a class="B" href="{{ path('Game/Market/Sell') }}">Sell</a> -
        <a class="B" href="{{ path('Game/Market/PlaceOffer') }}">Place offer</a> -
        <a class="B" href="{{ path('Game/Market/Offers') }}">Your offers</a>
    </p>

    <table class="table text-center">
        <tr>
            <td class="tabletop" colspan="5"><b>Your offers</b> </td>
        </tr>
        <tr>
            <td><b>Type</b></td>
            <td><b>Resource</b></td>
            <td><b>Amount</b></td>
            <td><b>Price</b></td>
            <td><b>Cancel</b></td>
        </tr>

        {% for item in marketItems %}
            <tr>
                <td>{{ item.type }}</td>
                <td>{{ item.gameResource }}</td>
                <td>{{ item.amount }}</td>
                <td>{{ item.price }}</td>
                <td><a class="B" href="{{ path('Game/Market/Cancel/Order', {'marketItemId': item.id}) }}"><b>Cancel</b></a></td>
            </tr>
        {% else %}
            <tr>
                <td colspan="5">No offers...</td>
            </tr>
        {% endfor %}
    </table>
{% endblock %}
